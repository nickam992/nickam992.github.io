(function() {
    /**
     * @ngdoc overview
     * @name player.notes
     * @description
     *
     * # Player Notes Module
     *
     * The `player.notes` module extracts note contnet into a following element which scrolls with
     * the content.
     *
     */
    'use strict';

    angular
        .module('player.notes', []);
})();
